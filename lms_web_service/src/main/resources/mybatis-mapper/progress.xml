<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sky.lms_web_service.mapper.ProgressMapper">

    <insert id="saveVideoProgress" parameterType="com.sky.lms_web_service.dto.Section_Progress">
        INSERT INTO section_progress (con_num, lec_num, user_no, learning_time, fnl_s, max_s)
        VALUES (#{conNum}, #{lecNum}, #{userNo}, #{learningTime}, #{fnlS}, #{maxS})
    </insert>

    <insert id="saveLastWatchedTime" parameterType="com.sky.lms_web_service.dto.Section_Progress">
        INSERT INTO section_progress (conNum, lecNum, userNo, learningTime, maxS)
        VALUES (#{conNum}, #{lecNum}, #{userNo}, #{learningTime}, #{maxS})
            ON DUPLICATE KEY UPDATE learningTime = #{learningTime}, maxS = #{maxS}
    </insert>
</mapper>