<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sky.lms_web_service.mapper.ProgressMapper">

    <insert id="saveVideoProgress" parameterType="com.sky.lms_web_service.dto.Section_Progress">
        INSERT INTO section_progress (conNum, lecNum, userNo, learningTime, fnlS, maxS)
        VALUES (#{conNum}, #{lecNum}, #{userNo}, #{learningTime}, #{fnlS}, #{maxS})
    </insert>

    <select id="getVideoProgress" resultType="java.util.Map">
        SELECT learningTime, fnlS, maxS FROM section_progress WHERE conNum = #{conNum}
    </select>
</mapper>