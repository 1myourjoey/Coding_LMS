<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yong.lmstest.repository.VideoMapper">

    <select id="findAllVideos" resultType="com.yong.lmstest.model.videoDto">
        SELECT * FROM contents_manage
    </select>

    <insert id="saveVideo" parameterType="com.yong.lmstest.model.videoDto">
        INSERT INTO contents_manage (con_num, con_name, con_desc, video_id, con_play_time, lec_name, thumb_url)
        VALUES (#{conNum}, #{conName}, #{conDesc}, #{videoId}, #{conPlayTime}, #{lecName}, #{thumbUrl});
    </insert>


</mapper>